# [JRNOS-EXX-SXX] Story Title
# Epic: EXX | Sprint: WX-X | Size: S/M/L | Points: X

## ─────────────────────────────────────────────────────────────────
## TABLE OF CONTENTS — Auto-generated from EPIC.md when creating this packet
## ─────────────────────────────────────────────────────────────────
# Generated by sub-agent reading .context/epics/EXX/EPIC.md dependency table.
# Gives instant situational awareness: phase, priority, dependencies, parallel companions.

### This story
# Story:    JRNOS-EXX-SXX · [Title]
# Epic:     EXX · [Epic title]
# Phase:    N of M (e.g., "Phase 3 of 8")
# Priority: HIGH/MEDIUM/LOW
#   HIGH   = this story blocks 3+ other stories or is CRITICAL
#   MEDIUM = this story blocks 1-2 other stories
#   LOW    = no stories are blocked by this one
# Size:     S/M/L · [estimate in hours]
# Critical: YES/NO · [C01-C10 or H01-H13 finding if YES]

### Epic execution map (YOU ARE HERE ←)
# Full visual map of all phases in this epic.
# ──►  = sequential phase (must complete before next)
# ◆──  = parallel phase (multiple stories run simultaneously in worktrees)
# ├──  = parallel story branch
# └──  = last parallel story branch
# [DONE] / [in progress] / [not started] / [BLOCKED — needs Phase N first]
#
# Example:
# Phase 1 ──► EXX-S01  First story                   [sequential · S]
# Phase 2 ──► EXX-S02  Second story                  [sequential · M]
# Phase 3 ◆── EXX-S03  This story                    ← YOU ARE HERE · M
#          ├── EXX-S04  Parallel companion A          [parallel · M]
#          └── EXX-S05  Parallel companion B          [parallel · M]
# Phase 4 ──► EXX-S06  Downstream story              [BLOCKED — needs all of Phase 3]

### Immediate dependencies
# Blocked by (must be merged to develop BEFORE starting this story):
#   EXX-SXX · [Story title] — provides: [what this story needs from it]
#
# Blocking (cannot start until THIS story is merged to develop):
#   EXX-SXX · [Story title] — needs: [what it gets from this story]
#   (and possibly other parallel companions too)

### Parallel execution
# Stories that CAN run simultaneously in separate git worktrees:
#   EXX-SXX · [Title]
#   EXX-SXX · [Title]
#
# To create worktrees for this parallel phase:
#   /epic EXX PHASE[N]
#
# FILES OWNED by this story (verify no overlap before starting parallel work):
#   [list from FILES OWNED section below]

## ─────────────────────────────────────────────────────────────────

## CRITICAL: NO
# YES if this resolves a production finding (C01-C10, H01-H13, M01-M10).
# If YES: "YES — Resolves C01 (exam answer auto-save)"
# If YES: /plan-eng will research best practices before planning.
# CRITICAL stories require /careful mode before implementation.

## RESEARCH REQUIRED: NO
# YES + list specific URLs if the implementation involves complex/unfamiliar patterns.
# Example:
# YES
# - https://www.inngest.com/docs/features/inngest-functions/steps-and-tooling/step-waitforevent
# - https://github.com/pgvector/pgvector#hnsw
# - https://dl.acm.org/doi/10.3233/978-1-60750-799-4-196 (BKT algorithm)

## PARALLELIZABLE: YES
# YES = can run in a separate git worktree simultaneously with other stories in this phase.
# Must list FILES OWNED if YES (parallel stories must not share files).

## SCHOLARLY + DS&A CONTEXT
# Required for stories touching: mastery, IRT, generation, coverage, exam delivery,
# security-critical paths, analytics, or LOD enrichment.
# Leave: # N/A — for pure infrastructure stories (CI setup, config-only changes).
# Source document: tests/fixtures/journey-os-algorithms-scholarly.html
#
# Format (one block per algorithm/structure/foundation used):
#
# ALGORITHM: [name]
#   source:      journey-os-algorithms-scholarly.html §[V section]
#   complexity:  time O(?) · space O(?)
#   parameters:  [exact values — never leave as placeholders]
#   security:    [security property or N/A]
#   invariants:  [must be true after this story ships — verified by test]
#
# FOUNDATION: [Author, Year]
#   claim:   [what the research shows]
#   rule:    [implementation consequence for this story]
#   applies: [specific function/module/table]
#
# DATA STRUCTURE: [name]
#   source:  journey-os-algorithms-scholarly.html §VI
#   ops:     [which operations this story uses and their complexity]
#   where:   [package/file/table]
#
# Example for E08 BKT story:
# ALGORITHM: BKT mastery update
#   source:      §II · BKT Update specification
#   complexity:  O(1) time · O(1) space
#   parameters:  p_init=0.10, p_transit=0.20, p_slip=0.10, p_guess=0.20, threshold=0.95
#   security:    N/A
#   invariants:
#     - p_mastery ∈ (0,1) always (never exact 0 or 1, clamped to [0.001, 0.999])
#     - BKT NEVER runs during exam delivery (only runs after session finalizes)
#     - concept_mastery updated atomically via RPC, never sequential INSERTs (AP-17)
# FOUNDATION: Corbett & Anderson, 1995
#   claim:   BKT requires ≥3 responses before p_mastery is reliable
#   rule:    Never apply clinical tier override until review_count ≥ 3
#   applies: packages/ai/src/mastery/bkt.ts → bkt_update()

## DEPENDENCIES: []
# Story IDs that MUST complete (merged to develop) before starting this story.
# [] = no dependencies (Phase 1 story, can start immediately).
# Example: [E03-S01, E03-S02]

## FILES OWNED:
# Every file this story CREATES or MODIFIES. Required when PARALLELIZABLE: YES.
# Parallel stories must not share any files (causes merge conflicts).
# Example:
# - apps/inngest/functions/p1-step-01b-ocr.ts  (NEW)
# - packages/ai/prompts/ocr.md                 (NEW)
# - apps/web/app/api/lectures/upload/route.ts  (MODIFY — add OCR step call)
# - tests/E03/E03-S03.pipeline.test.ts         (NEW)

---

## Summary
One sentence: what does this story build?

## User story
As a [faculty/student/admin], I want to [action], so that [outcome].

## Why this matters
What breaks in production if this story is implemented incorrectly?
If CRITICAL: what specific incident does this prevent?

## Acceptance criteria
- [ ] Specific, testable, binary criterion 1
- [ ] Specific, testable, binary criterion 2
- [ ] Specific, testable, binary criterion 3

## Canonical decisions in play
| Rule | What it means for this story |
|---|---|
| Rule 1 (C05) | neo4jQuery for all Neo4j writes — wrapper injects institution_id |
| Rule N | [applicable decision + specific implication] |

## Schema changes
```sql
-- None
-- OR: exact DDL including IF NOT EXISTS for safety
-- After adding: pnpm db:types to regenerate database.generated.ts
```

## Neo4j nodes created/modified
| Node type | Properties set | Created or modified? |
|---|---|---|
| SubConcept | uuid, name, cui, evidence_grade | Created |

## Neo4j edges created/traversed
| Edge | From → To | Properties | Notes |
|---|---|---|---|
| TEACHES | ContentChunk → SubConcept | evidence_grade:'C', evidence_weight:0.65 | Rule 9: both properties required |

## Inngest event context
Function: journey/[function-name]
Step: step.run('[step-name]', ...) — position in step sequence: after [X], before [Y]
Events triggered: [journey/event.name with data shape]
Events consumed: [journey/event.name]
onFailure: [what happens on failure — log to dead letter? continue? retry?]

## API contract
```
None — sub-step of existing pipeline
OR:
POST /app/api/[route]
Headers: Authorization: Bearer [token]
Request: { field: type }
Response: { data: { field: type }, error: null }
Error: { data: null, error: { code: string, message: string } }
```

## Code patterns to follow
```typescript
// Show exact pattern from packages/ or existing code
// Reference the relevant Rule numbers from CLAUDE.md
```

## Code patterns to AVOID
```typescript
// ✗ AP-XX: [antipattern name] — [what production failure it causes]
// ✗ AP-XX: [antipattern name] — [what production failure it causes]
// Full examples in .context/canonical/antipatterns.md
```

## Seed data dependencies
Must be seeded and validate-seed ✓ before testing this story:
- None
- OR: 02-misconception-categories.ts (provides: 15 MisconceptionCategory nodes)

## Automated tests (Playwright — runs during /validate)
```typescript
// tests/EXX/EXX-SXX.[api|pipeline|ui|seed].test.ts
// ACTUAL RUNNABLE TEST CODE — not a description of tests

// Pipeline story pattern:
test('Acceptance: [criterion 1]', async ({ request }) => {
  const { token } = await getTestToken(request, 'faculty')
  // trigger → waitForStatus → assertNeo4j → Supabase check
})

// API story pattern:
test('POST /api/[route] returns 200 and creates expected record', async ({ request }) => {
  const { token } = await getTestToken(request, 'faculty')
  const res = await request.post('/app/api/[route]', {
    headers: { Authorization: `Bearer ${token}` },
    data: { /* request body */ }
  })
  expect(res.status()).toBe(200)
  const { data } = await res.json()
  expect(data.[field]).toBe([expected value])
})

// Negative test: failure mode
test('Negative: [failure mode description]', async ({ request }) => {
  // test the error path
})
```

## Rollback procedure
If implementation fails mid-way:
1. [Exact SQL to reverse migration if applied: DROP COLUMN / DROP TABLE]
2. [Exact Cypher to remove any Neo4j nodes created]
3. git stash && git checkout develop

## Definition of done
- [ ] All acceptance criteria verified
- [ ] /validate passes: TypeScript ✓, ESLint ✓ (0 violations), unit tests ✓, Playwright tests ✓
- [ ] /qa confirmed by user (their message goes in the commit)
- [ ] No canonical violations (AP-01 through AP-17)
- [ ] Supabase migration applied to staging (if applicable) + pnpm db:types run
- [ ] Prompt committed to packages/ai/prompts/ (if any new LLM prompt created)
- [ ] PR merged to develop with user verification in commit message
